<h1><%= @category.title %></h1>

<br>
<ul>
<% @items.each do |item| %>
    <li>$<%= item.price.to_f/100 %>, <%= link_to item.title, item_path(item) %> | <% if user_signed_in? %><%= button_to 'Add to Cart', line_items_path(item_id: item) %><% end %></li>
<% end %>
</ul>